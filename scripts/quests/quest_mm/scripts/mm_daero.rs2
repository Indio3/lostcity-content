[opnpc1,mm_daero]
if(%mm_narnode < ^narnode_received_orders) {
    ~mesbox("Daero seems like he is enjoying himself too much to talk.");
    return;
}

[debugproc,test]
runanim(null);
walktrigger(reset_run);
if(p_finduid(uid) = true) p_walk(movecoord(coord, 5, 0, 0));

[walktrigger,reset_run]
~update_bas;